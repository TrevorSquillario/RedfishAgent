services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru
    container_name: redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - redfishagent

  redfishagent:
    build:
      context: .
      dockerfile: redfishagent/Dockerfile
    image: redfishagent:latest
    container_name: redfishagent
    ports:
      - "8081:8000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "python -c 'import urllib.request,sys; urllib.request.urlopen(\"http://127.0.0.1:8000/healthz\")'"]
      interval: 10s
      timeout: 3s
      retries: 5
    volumes:
      - ./redfishagent/config/config.yaml:/config/config.yaml:ro
      - ./inventory.yaml:/config/inventory.yaml:ro
      - ./redfishagent:/app:cached
      - ~/redfishagent/input:/input:ro
      - ~/.cache/datalab:/root/.cache/datalab # Model cache for marker-pdf
    environment:
      - LOG_LEVEL=DEBUG
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      #- REDIS_PASSWORD=
      - POSTGRES_DB=redfishagent_db
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_HOST=pgvector
      - POSTGRES_PORT=5432
      - OPENAI_API_BASE=http://192.168.0.30:8090 # vllm
      - OPENAI_MODEL=google/gemma-4-26B-A4B-it
      #- OPENAI_MODEL=nvidia/Nemotron-Cascade-2-30B-A3B
      # Leave OPENAI_API_KEY unset for local OpenAI-compatible servers like vllm
      - PHOENIX_COLLECTOR_ENDPOINT=http://phoenix:4317
      - PROMETHEUS_URL=http://victoriametrics:8428
      - REDIS_STREAM=redfish_alerts
      - SLACK_APP_TOKEN=${SLACK_APP_TOKEN}
      - SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN}
      - SLACK_CHANNEL_ID=${SLACK_CHANNEL_ID}
    networks:
      - redfishagent
    depends_on:
      redis:
        condition: service_healthy
      idrac-redfish-mcp:
        condition: service_healthy

  pgvector:
    image: pgvector/pgvector:pg17 # PostgreSQL with pgvector support
    container_name: pgvector
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: redfishagent_db
    ports:
      - "5432:5432"
    #volumes:
      #- ~/app/pgvector:/var/lib/postgresql/data
      #- ./postgres/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    networks:
      - redfishagent

  redfish-listener:
    build:
      context: ./redfish-listener-go
    image: redfishagent/redfish-listener:latest
    container_name: redfish-listener
    ports:
      #- "8443:8443"
      - "8080:8080"
    restart: unless-stopped
    #volumes:
    #  - ~/redfish-mockups/ssl:/certs
    environment:
      - LISTENER_DEST=http://192.168.0.30:8080/redfish/events
      #- LISTENER_DEST=https://127.0.0.1:8443/redfish/events
      #- LISTENER_CERT=/certs/server.crt
      #- LISTENER_KEY=/certs/server.key
      - REDIS_URL=redis://redis:6379
      - REDIS_STREAM=redfish_alerts
      - INVENTORY_URL=http://redfishagent:8000/api/inventory/list
    # command: ["python", "src/redfish_listener.py", "-c", "src/config.ini"]
    depends_on:
      redis:
        condition: service_healthy
      redfishagent:
        condition: service_healthy
    networks:
      - redfishagent

  idrac-exporter:
    #image: ghcr.io/mrlhansen/idrac_exporter:latest
    #image: idrac_exporter:latest
    image: idrac_exporter:mr 
    container_name: idrac-exporter
    restart: unless-stopped
    environment:
      CONFIG_DEFAULT_USERNAME: user
      CONFIG_DEFAULT_PASSWORD: calvin
      CONFIG_DEFAULT_SCHEME: https
      CONFIG_DEFAULT_PORT: 443
      CONFIG_DEFAULT_USE_BASIC_AUTH: true
    volumes:
      - ./idrac-exporter/config.yaml:/etc/prometheus/idrac.yml:ro
    ports:
      - "9348:9348"
    networks:
      - redfishagent

  idrac-redfish-mcp:
    build:
      context: ./idrac-redfish-mcp
      dockerfile: Dockerfile
    image: idrac-redfish-mcp:latest
    container_name: idrac-redfish-mcp
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: "1"
      LOG_LEVEL: DEBUG
    volumes:
      - ./idrac-redfish-mcp/src:/app
      - ./redfish-mockup-server/src/example_screenshots:/app/example_screenshots:ro
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/health || exit 1"]
      interval: 1s
      timeout: 1s
      retries: 25
      start_period: 0s
    ports:
      - "8092:8080"
    networks:
      - redfishagent

  # VictoriaMetrics instance, a single process responsible for
  # storing metrics and serve read requests.
  victoriametrics:
    image: victoriametrics/victoria-metrics:v1.138.0
    ports:
      - 8428:8428
      - 8089:8089
      - 8089:8089/udp
      - 2003:2003
      - 2003:2003/udp
      - 4242:4242
    #volumes:
    #  - vmdata:/storage
    command:
      - "--storageDataPath=/storage"
      - "--graphiteListenAddr=:2003"
      - "--opentsdbListenAddr=:4242"
      - "--httpListenAddr=:8428"
      - "--influxListenAddr=:8089"
    restart: always
    networks:
      - redfishagent
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8428/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    depends_on:
      redfishagent:
        condition: service_healthy

  #  Metrics collector.
  #  It scrapes targets defined in --promscrape.config
  #  And forward them to --remoteWrite.url
  vmagent:
    image: victoriametrics/vmagent:v1.138.0
    ports:
      - 8429:8429
    volumes:
      #- vmagentdata:/vmagentdata
      - ./victoria-metrics/prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - "--promscrape.config=/etc/prometheus/prometheus.yml"
      - "--remoteWrite.url=http://victoriametrics:8428/api/v1/write"
    restart: always
    networks:
      - redfishagent
    depends_on:
      - "victoriametrics"

  # vmalert executes alerting and recording rules
  vmalert:
    image: victoriametrics/vmalert:v1.138.0
    depends_on:
      - "victoriametrics"
      - "alertmanager"
    ports:
      - 8880:8880
    volumes:
      - ./victoria-metrics/rules/alerts.yml:/etc/alerts/alerts.yml
      - ./victoria-metrics/rules/alerts-health.yml:/etc/alerts/alerts-health.yml
      - ./victoria-metrics/rules/alerts-vmagent.yml:/etc/alerts/alerts-vmagent.yml
      - ./victoria-metrics/rules/alerts-vmalert.yml:/etc/alerts/alerts-vmalert.yml
      #- ./victoria-metrics/rules/alerts-redfish-alerts.yml:/etc/alerts/alerts-redfish-alerts.yml
    command:
      - "--datasource.url=http://victoriametrics:8428/"
      - "--remoteRead.url=http://victoriametrics:8428/"
      - "--remoteWrite.url=http://vmagent:8429/"
      - "--notifier.url=http://alertmanager:9093/"
      - "--rule=/etc/alerts/*.yml"
      # display source of alerts in grafana
      - "--external.url=http://127.0.0.1:3000" #grafana outside container
      - '--external.alert.source=explore?orgId=1&left={"datasource":"VictoriaMetrics","queries":[{"expr":{{.Expr|jsonEscape|queryEscape}},"refId":"A"}],"range":{"from":"{{ .ActiveAt.UnixMilli }}","to":"now"}}'
    restart: always
    networks:
      - redfishagent

  # alertmanager receives alerting notifications from vmalert
  # and distributes them according to --config.file.
  alertmanager:
    image: prom/alertmanager:v0.28.1
    volumes:
      - ./victoria-metrics/alertmanager.yml:/config/alertmanager.yml
    command:
      - "--config.file=/config/alertmanager.yml"
    ports:
      - 9093:9093
    restart: always
    networks:
      - redfishagent

  grafana:
    image: grafana/grafana:12.2.0
    depends_on:
      - "victoriametrics"
    ports:
      - 3000:3000
    restart: always
    environment:
      - GF_PLUGINS_PREINSTALL=yesoreyeram-infinity-datasource
    volumes:
      #- grafanadata:/var/lib/grafana
      - ./grafana/provisioning/datasources/prometheus/single.yml:/etc/grafana/provisioning/datasources/single.yml
      - ./grafana/provisioning/datasources/infinity/single.yml:/etc/grafana/provisioning/datasources/infinity-single.yml
      - ./grafana/provisioning/dashboards_config:/etc/grafana/provisioning/dashboards
      - ./grafana/provisioning/dashboards/victoriametrics.json:/var/lib/grafana/dashboards/vm.json
      - ./grafana/provisioning/dashboards/vmagent.json:/var/lib/grafana/dashboards/vmagent.json
      - ./grafana/provisioning/dashboards/vmalert.json:/var/lib/grafana/dashboards/vmalert.json
      - ./grafana/provisioning/dashboards/alert-statistics.json:/var/lib/grafana/dashboards/alert-statistics.json
      - ./grafana/provisioning/dashboards/metrics-explorer.json:/var/lib/grafana/dashboards/metrics-explorer.json

  # LLM Tracing and Debugging Tool
  phoenix:
    image: arizephoenix/phoenix:latest
    ports:
      - "6006:6006"  # UI and OTLP HTTP collector
      - "4317:4317"  # OTLP gRPC collector
    restart: always
    networks:
      - redfishagent



networks:
  redfishagent:
    driver: bridge