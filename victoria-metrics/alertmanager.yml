route:
  receiver: blackhole
  routes:
    - match:
        severity: "critical"
      receiver: webhook-receiver

receivers:
  - name: blackhole
  - name: webhook-receiver
    webhook_configs:
      - url: "http://agentredfish:8000/api/webhook/alerts/prometheus"